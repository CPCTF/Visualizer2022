export interface UserRaw {
  id: string
  displayName: string
  iconPath: string
}

export interface UserRankingRaw {
  id: string
  displayName: string
  iconPath: string
  point: number
  ranking: number
}
